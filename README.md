===== iSpring → Google Sheets (UA, PROD, CONSOLIDATED + Single-Choice "answers" Fix) =====
// - «Результати» + «Детальні відповіді» (+ XML у підпапці поруч із книгою)
// - Оцінки L/M рахуються на СЕРВЕРІ за «Шкала» (жодних формул у клітинках; стійко до локалі)
// - «Шкала» створюється/ремонтується автоматично; є меню для перерахунку оцінок
// - «Перегляд» у «Детальні відповіді» — RichText‑посилання (не формула)
// - Веб‑перегляд питання: /exec?view=q&fid=... ; сирий XML: /exec?view=xml&fid=...
// - GET‑тригер створення «Шкала»: /exec?view=mkScale
